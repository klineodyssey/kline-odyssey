name: TX Engine Autopilot

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  sanity-check:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "workflow graph OK"
