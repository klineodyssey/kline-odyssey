# Cursor Cloud on-demand capacity

STATUS: `SUSPENDED_BY_HUMAN_COST_DECISION`

Human Authority 沈英明 deferred Cursor Cloud subscription renewal until the
Company has high workload that justifies paid external capacity. Cursor Cloud
is not part of the normal active cloud workforce and HTTP 403 must not be
retried. Local free Cursor functionality and historical worker evidence remain
available.

The executable workflow is manual-only and records the suspension state. It
does not read `CURSOR_API_KEY`, call an external endpoint, launch an agent, run a
watchdog or create a charge. Normal 24H operations use `codex-gm-01`,
`chatgpt-01`, GitHub Actions, repository-native handoffs, hourly GM patrol and
Pages.

If future workload requires parallel external capacity, the GM must first
report provider, purpose, expected cost and expected output. Reactivation then
requires a new Human cost decision and a separately reviewed bounded workflow.
Mainnet, funds, Treasury, payment, signer, LP, governance, KYC and secret export
remain independently protected.
