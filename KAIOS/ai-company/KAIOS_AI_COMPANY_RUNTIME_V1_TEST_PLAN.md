# KAIOS AI Company Runtime V1 Test Plan

Status: `SPECIFICATION_GATE`

## Specification Gates

- parse every JSON Schema with duplicate-key rejection;
- require unique Draft 2020-12 IDs and local reference targets;
- require 21 divisions, 16 feasibility gates and 9 project templates;
- require request clarification and visible assumption contracts;
- require farm dependency blocking and candidate-only life workflow;
- require simulated-contract and read-only API boundaries;
- preserve Chapter 136 lineage conflict and source-underspecified gaps.

## Runtime Gates

- requests, clarification, analysis, approval and rejection;
- predecessor graph, cycle rejection, critical path and no skipping;
- BOM, worker timeline, equipment, energy, routes and warehouse capacity;
- budget, commitment, spending, balanced ledger and insolvency;
- explicit time, pause/resume, failure, inspection, rework and changes;
- delivery, acceptance, maintenance and closeout;
- total/physical/digital/compute/review/procurement/worker/finance capacity;
- fishpond, house, blocked farm, life-package and software demonstrations;
- deterministic state/event hashes, serialization, replay, export/import/reset;
- World Viewer loading/empty/error/retry and responsive accessibility;
- static API methods `GET/HEAD`, mutation endpoints `FALSE`;
- Company Boot and all named regression suites.

## Security Gates

`P0=0`, unresolved `P1=0`, unresolved `P2=0`, secrets `0`, protected
violations `0`, wallet access `false`, real KGEN `false`, on-chain transfer
`false`, Production authority `false`, external execution `false`, CURRENT
changes `0`, Constitution source changes `0`.
