# KAIOS Canonical Life Validation Plan

Status: `ACTIVE_FOR_PR66_SPECIFICATION_VALIDATION`

Contract tests validate required files, all 17 Life types, all approved
extensions, exact applicability and tradeability sets, nine universal taxonomy
ranks, 19-layer biological compatibility, Universal Core completeness,
type-to-extension constraints, separated rights, physics/economy invariants,
Schema V2 compatibility references, template completeness and disabled runtime,
wallet, on-chain and K11520 authority.

Repository gates include all JSON parsing, Draft 2020-12 schema compilation,
template validation, Company Boot, identity, organism V2, K280, PR #64, PR #65,
static acceptance, Markdown links, UTF-8, BOM, corruption, secrets, protected
paths and `git diff --check`.

Merge requires `P0=0`, unresolved `P1=0`, and unresolved `P2=0`.
