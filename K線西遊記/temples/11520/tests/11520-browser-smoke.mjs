import assert from 'node:assert/strict';
import fs from 'node:fs/promises';
import {chromium} from 'playwright';

const ARTIFACT_DIR='artifacts/11520-visual-qa';
await fs.mkdir(ARTIFACT_DIR,{recursive:true});

const browser=await chromium.launch({headless:true});
const page=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true});
const errors=[];page.on('pageerror',e=>errors.push(String(e)));
await page.goto('http://127.0.0.1:4173/K%E7%B7%9A%E8%A5%BF%E9%81%8A%E8%A8%98/temples/11520/game-5d.html',{waitUntil:'domcontentloaded',timeout:30000});
await page.waitForTimeout(1500);
assert.deepEqual(errors,[],'boot page errors: '+errors.join('\n'));
if(await page.locator('#intro11520').count()){
  const enter=page.locator('#enter11520');
  if(await enter.count()&&await enter.isVisible().catch(()=>false))await enter.click({timeout:1500}).catch(()=>{});
  await page.locator('#intro11520').waitFor({state:'hidden',timeout:2500}).catch(()=>{});
  assert.equal(await page.locator('#intro11520').isVisible().catch(()=>false),false,'intro must dismiss by tap or automatic fail-open');
  await page.waitForTimeout(250)
}

const assertVisible=async selector=>{const loc=page.locator(selector);const d=await loc.evaluate(el=>{const r=el.getBoundingClientRect(),s=getComputedStyle(el);return{display:s.display,visibility:s.visibility,pointer:s.pointerEvents,w:r.width,h:r.height,x:r.x,y:r.y,right:r.right,bottom:r.bottom,text:(el.textContent||'').trim()}});assert.ok(d.w>0&&d.h>0&&d.display!=='none'&&d.visibility!=='hidden',`${selector} not visible: ${JSON.stringify(d)}`);return d};
const xyz=async()=>{const t=await page.locator('#xyz').textContent();const m=String(t).match(/X\s*(-?\d+(?:\.\d+)?)\s*·\s*Y\s*(-?\d+(?:\.\d+)?)\s*·\s*Z\s*(-?\d+(?:\.\d+)?)/);assert.ok(m,'XYZ HUD parse failed: '+t);return{x:+m[1],y:+m[2],z:+m[3]}};
const dragReal=async(selector,toX,toY,hold=260)=>{const b=await page.locator(selector).boundingBox();assert.ok(b,selector+' missing box');const sx=b.x+b.width*.5,sy=b.y+b.height*.5,tx=b.x+b.width*toX,ty=b.y+b.height*toY;await page.mouse.move(sx,sy);await page.mouse.down();await page.mouse.move(tx,ty,{steps:5});await page.waitForTimeout(hold);await page.mouse.up()};
const dragVertical=async(selector,toY,pointerId)=>{const b=await page.locator(selector).boundingBox();assert.ok(b,selector+' missing box');const p=(y,buttons=1)=>({pointerId,pointerType:'touch',clientX:b.x+b.width*.5,clientY:b.y+b.height*y,buttons});await page.dispatchEvent(selector,'pointerdown',p(.5));await page.dispatchEvent(selector,'pointermove',p(toY));await page.waitForTimeout(80);await page.dispatchEvent(selector,'pointerup',p(toY,0))};
let railPointerId=1800;const pressRail=async(sign,baseline)=>{const b=await page.locator('#yControl').boundingBox();assert.ok(b,'#yControl missing box');const pointerId=railPointerId++,clientX=b.x+b.width*.5,clientY=b.y+b.height*.5,targetY=sign>0?b.y+b.height*.14:b.y+b.height*.86;await page.dispatchEvent('#yControl','pointerdown',{pointerId,pointerType:'touch',clientX,clientY,buttons:1});await page.dispatchEvent('#yControl','pointermove',{pointerId,pointerType:'touch',clientX,clientY:targetY,buttons:1});try{await page.waitForFunction(({sign,baseline})=>{const text=document.querySelector('#xyz')?.textContent||'',match=String(text).match(/Y\s*(-?\d+(?:\.\d+)?)/);if(!match)return false;const current=Number(match[1]);return sign>0?current>baseline:current<baseline},{sign,baseline},{timeout:3000,polling:50})}finally{await page.dispatchEvent('#yControl','pointerup',{pointerId,pointerType:'touch',clientX,clientY:targetY,buttons:0})}await page.waitForTimeout(120)};
const overlap=(a,b)=>Math.max(0,Math.min(a.right,b.right)-Math.max(a.x,b.x))*Math.max(0,Math.min(a.bottom,b.bottom)-Math.max(a.y,b.y));
const rect=async selector=>page.locator(selector).evaluate(el=>{const r=el.getBoundingClientRect();return{x:r.x,y:r.y,right:r.right,bottom:r.bottom,w:r.width,h:r.height}});

for(const id of ['joy','attack','skill','dodge','gameModeToggle'])await assertVisible('#'+id);await assertVisible('#knob img');
let a=await xyz();await dragReal('#joy',.9,.5);let b=await xyz();assert.ok(b.x>a.x,'right must X+');await assertVisible('#knob img');await page.screenshot({path:`${ARTIFACT_DIR}/11520-mobile-x-right.png`,fullPage:true});
const canvasTransform=await page.locator('#three').evaluate(el=>getComputedStyle(el).transform);assert.ok(canvasTransform&&canvasTransform!=='none'&&/^matrix\(-1(?:\.0+)?,/.test(canvasTransform),`X visual mirror missing: ${canvasTransform}`);
await dragReal('#joy',.1,.5);let c=await xyz();assert.ok(c.x<b.x,'left must X-');await assertVisible('#knob img');await page.screenshot({path:`${ARTIFACT_DIR}/11520-mobile-x-left.png`,fullPage:true});await dragReal('#joy',.5,.1);let d=await xyz();assert.ok(d.z>c.z,'up must Z+');await dragReal('#joy',.5,.9);let e=await xyz();assert.ok(e.z<d.z,'down must Z-');await assertVisible('#knob img');

/* Bottom-right gear replaces legacy all-or-nothing clean mode with explicit per-surface switches. */
await page.locator('#gameModeToggle').click({timeout:3000});await page.waitForTimeout(180);await assertVisible('#k11520UiSettings');
assert.equal(await page.locator('body').evaluate(el=>el.classList.contains('game-clean-mode')),false,'legacy clean mode must stay retired');
for(const id of ['walletToggle','dockToggle','orderFire','tradeSword','flat','aiChatButton','bgmButton'])await assertVisible('#'+id);await assertVisible('#yThumb');assert.match(await page.locator('#yThumb').evaluate(el=>getComputedStyle(el).backgroundImage),/wukong-y-control\.jpg/i,'remaining-axis thumb must show approved Wukong artwork');await assertVisible('#backpackButton.bagRelocatedV258');
const mapSwitch=page.locator('#k11520UiSettings [data-ui-key="map"]');await mapSwitch.click();await page.waitForTimeout(40);assert.equal(await page.locator('.minimapWrap').isVisible(),false,'map switch left/off must hide minimap');await mapSwitch.click();await page.waitForTimeout(40);assert.equal(await page.locator('.minimapWrap').isVisible(),true,'map switch right/on must restore minimap');await page.screenshot({path:`${ARTIFACT_DIR}/11520-mobile-ui-settings.png`,fullPage:true});await page.locator('#k11520UiSettingsClose').click();

const yPointer=await page.evaluate(()=>{const shell=document.querySelector('#yControl'),track=document.querySelector('#yControl .track'),knob=document.querySelector('#yThumb');assertElement(shell,'#yControl');assertElement(knob,'#yThumb');return{shell:getComputedStyle(shell).pointerEvents,track:track?getComputedStyle(track).pointerEvents:null,knob:getComputedStyle(knob).pointerEvents,trackPresent:!!track};function assertElement(el,label){if(!(el instanceof Element))throw new Error(`${label} missing from live remaining-axis control`)}});assert.notEqual(yPointer.shell,'none','remaining-axis rail must stay interactive');if(yPointer.trackPresent)assert.notEqual(yPointer.track,'none','remaining-axis track must remain interactive when present');assert.notEqual(yPointer.knob,'none','remaining-axis thumb must remain interactive');
const yHit=await page.locator('#yThumb').evaluate(el=>{const r=el.getBoundingClientRect();return document.elementsFromPoint(r.x+r.width/2,r.y+r.height/2).slice(0,5).map(x=>({id:x.id,cls:x.className}))});assert.ok(yHit.some(x=>x.id==='yThumb'||x.id==='yControl'),`remaining-axis thumb is covered by another control: ${JSON.stringify(yHit)}`);
const walletToggleVisual=await assertVisible('#walletToggle');assert.ok(walletToggleVisual.text.length>0,'wallet toggle is visible but unlabeled');
const walletAnchor=await page.evaluate(()=>{const p=document.querySelector('#walletPanel')?.getBoundingClientRect(),toggle=document.querySelector('#walletToggle'),t=toggle?.getBoundingClientRect();return p&&t?{viewportPinned:toggle.parentElement===document.body,p:{x:p.x,y:p.y,right:p.right,bottom:p.bottom},t:{x:t.x,y:t.y,right:t.right,bottom:t.bottom}}:null});assert.equal(walletAnchor?.viewportPinned,true,`wallet toggle must use its current viewport-pinned anchor: ${JSON.stringify(walletAnchor)}`);assert.ok(walletAnchor.t.x>=330&&walletAnchor.t.right<=390,`wallet toggle must remain inside the right utility rail: ${JSON.stringify(walletAnchor)}`);
const y0=(await xyz()).y;await pressRail(1,y0);const y1=(await xyz()).y;assert.ok(y1>y0,'Y up must increase altitude');await pressRail(-1,y1);const y2=(await xyz()).y;assert.ok(y2<y1,'Y down must decrease altitude');
await assertVisible('#lotsThumb img');await assertVisible('#cThumb img');
const l0=await page.locator('#lotsRead').textContent();await dragVertical('#lotsControl',.1,71);const l1=await page.locator('#lotsRead').textContent();assert.notEqual(l1,l0,'lots must change');await page.waitForTimeout(120);assert.equal(await page.locator('#lotsRead').textContent(),l1,'lots must retain');
await dragVertical('#lotsControl',.999,73);await page.waitForTimeout(80);assert.equal((await page.locator('#lotsRead').textContent()).trim(),'0口','lots minimum must be visible 0口');
const c0=await page.locator('#cRead').textContent();await dragVertical('#cControl',.1,72);const c1=await page.locator('#cRead').textContent();assert.notEqual(c1,c0,'C must change');await page.waitForTimeout(120);assert.equal(await page.locator('#cRead').textContent(),c1,'C must retain');
await dragVertical('#cControl',.999,74);await page.waitForTimeout(80);const cZero=(await page.locator('#cRead').textContent()).trim();assert.match(cZero,/^0C/,'C minimum must be 0');assert.match(cZero,/靜止/,'C=0 must visibly say 靜止');
const lotsReadRect=await rect('#lotsRead'),lotsThumbRect=await rect('#lotsThumb'),cReadRect=await rect('#cRead'),cThumbRect=await rect('#cThumb');assert.equal(overlap(lotsReadRect,lotsThumbRect),0,'lots 0 readout is covered by the thumb image');assert.equal(overlap(cReadRect,cThumbRect),0,'C 0 readout is covered by the thumb image');
await page.screenshot({path:`${ARTIFACT_DIR}/11520-mobile-zero-controls.png`,fullPage:true});

if(await page.locator('#lookPad').count()){const look=await page.locator('#lookPad').evaluate(el=>({display:getComputedStyle(el).display,pointer:getComputedStyle(el).pointerEvents}));assert.ok(look.display==='none'||look.pointer==='none','lookPad still intercepts world')}
const centerHit=await page.evaluate(()=>document.elementsFromPoint(innerWidth*.5,innerHeight*.58).map(el=>({id:el.id,cls:el.className})).slice(0,10));assert.ok(centerHit.some(x=>x.id==='three'),`3D canvas unreachable: ${JSON.stringify(centerHit)}`);assert.equal(centerHit.some(x=>x.id==='lookPad'),false,'lookPad occupies center');

const visual=await page.evaluate(()=>{const box=s=>{const el=document.querySelector(s);if(!el)return null;const r=el.getBoundingClientRect(),cs=getComputedStyle(el);if(cs.display==='none'||cs.visibility==='hidden'||r.width<=0||r.height<=0)return null;return{x:r.x,y:r.y,right:r.right,bottom:r.bottom,w:r.width,h:r.height,cls:el.className,text:(el.textContent||'').trim()}};const visibleEmptyButtons=[...document.querySelectorAll('button')].filter(el=>{const r=el.getBoundingClientRect(),cs=getComputedStyle(el);if(cs.display==='none'||cs.visibility==='hidden'||r.width<28||r.height<28)return false;const hasVisual=(el.textContent||'').trim().length>0||!!el.querySelector('img,svg,canvas');return !hasVisual}).map(el=>({id:el.id,cls:el.className}));return{viewport:{w:innerWidth,h:innerHeight},boxes:{joy:box('#joy'),dodge:box('#dodge'),y:box('#yControl'),sliders:box('.sliderDock'),combat:box('#k11520MotionCombat'),bag:box('#backpackButton.bagRelocatedV258'),dock:box('#dock'),settings:box('#gameModeToggle'),ai:box('#aiChatButton'),bgm:box('#bgmButton'),wallet:box('#walletPanel'),walletToggle:box('#walletToggle'),lotsRead:box('#lotsRead'),cRead:box('#cRead')},visibleEmptyButtons,version:document.querySelector('.brandMetaV250 span:first-child')?.textContent||'',canvasTransform:getComputedStyle(document.querySelector('#three')).transform}});
const {boxes}=visual;for(const [name,v] of Object.entries(boxes))if(v)assert.ok(v.x>=-1&&v.right<=391&&v.y>=-1&&v.bottom<=845,`${name} outside viewport: ${JSON.stringify(v)}`);
if(boxes.joy&&boxes.dodge)assert.equal(overlap(boxes.joy,boxes.dodge),0,'left joystick overlaps dodge');if(boxes.y&&boxes.dock)assert.equal(overlap(boxes.y,boxes.dock),0,'Y control overlaps dock');if(boxes.y&&boxes.bag)assert.equal(overlap(boxes.y,boxes.bag),0,'Y control overlaps backpack');if(boxes.y&&boxes.combat)assert.equal(overlap(boxes.y,boxes.combat),0,'Y control overlaps combat dashboard');if(boxes.sliders&&boxes.combat)assert.equal(overlap(boxes.sliders,boxes.combat),0,'C/lots sliders overlap combat dashboard');
assert.ok(boxes.wallet,'wallet missing');assert.ok(String(boxes.wallet.cls).includes('collapsed'),'wallet must start collapsed for mobile world view');assert.ok(boxes.wallet.w<=50,`collapsed wallet became center blocker: ${JSON.stringify(boxes.wallet)}`);assert.ok(boxes.bag,'canonical living-cargo backpack missing before visual capture');assert.equal(visual.visibleEmptyButtons.length,0,'visible empty button(s): '+JSON.stringify(visual.visibleEmptyButtons));assert.match(visual.version,/V2\.6\.17/,'visible product version is stale: '+visual.version);

await page.screenshot({path:`${ARTIFACT_DIR}/11520-mobile-390x844.png`,fullPage:true});await fs.writeFile(`${ARTIFACT_DIR}/11520-mobile-layout.json`,JSON.stringify({capturedAt:new Date().toISOString(),...visual,centerHit,walletAnchor,zeroControls:{lots:'0口',c:cZero},yPointer,yHit},null,2));

await page.locator('#aiChatButton').click({timeout:3000});await page.waitForTimeout(30);assert.equal(await page.locator('#aiChatPanel').evaluate(el=>el.classList.contains('open')),true,'AI did not open');await page.locator('#aiClose').click({timeout:3000});await page.waitForTimeout(30);assert.equal(await page.locator('#aiChatPanel').evaluate(el=>el.classList.contains('open')),false,'AI did not close');
const toggleBefore=await page.locator('#walletToggle').boundingBox();assert.ok(toggleBefore);await page.locator('#walletToggle').click({timeout:3000});await page.waitForTimeout(240);assert.equal(await page.locator('#walletPanel').evaluate(el=>el.classList.contains('collapsed')),false,'wallet did not expand');await assertVisible('#walletConnect');await page.locator('#walletConnect').click({timeout:3000});await page.waitForTimeout(50);assert.ok((await page.locator('#walletMsg').textContent()).length>0,'wallet message missing');assert.equal(await page.locator('#walletLaunchSheet').evaluate(el=>el.classList.contains('open')),true,'wallet launcher fallback did not open');await page.locator('#walletToggle').click({timeout:3000});await page.waitForTimeout(240);assert.equal(await page.locator('#walletPanel').evaluate(el=>el.classList.contains('collapsed')),true,'wallet did not collapse');assert.equal(await page.locator('#walletLaunchSheet').evaluate(el=>el.classList.contains('open')),false,'wallet launcher fallback must close with its parent wallet');const toggleAfter=await page.locator('#walletToggle').boundingBox();assert.ok(toggleAfter);assert.ok(Math.abs(toggleAfter.x-toggleBefore.x)<16&&Math.abs(toggleAfter.y-toggleBefore.y)<16,'wallet toggle moved after collapse');
const walletCollapsedVisual=await assertVisible('#walletPanel');assert.ok(walletCollapsedVisual.w<=50&&walletCollapsedVisual.h<=50,`collapsed wallet remains a visible overlay: ${JSON.stringify(walletCollapsedVisual)}`);

assert.equal(await page.locator('#backpackButton.bagRelocatedV258').count(),1,'exactly one canonical standalone backpack required');
await page.locator('#backpackButton').click({timeout:3000});await page.waitForTimeout(40);assert.equal(await page.locator('#backpackPanel').evaluate(el=>el.classList.contains('open')),true,'living-cargo backpack did not open');await assertVisible('#backpackCaptureNearby');assert.match(await page.locator('#backpackPanel').textContent(),/LIFE_ID/,'backpack must explain LIFE_ID-preserving living cargo');assert.equal(await page.locator('#backpackClose').count(),0,'backpack must use the same stable toggle instead of a duplicate close control');await page.screenshot({path:`${ARTIFACT_DIR}/11520-mobile-backpack.png`,fullPage:true});await page.locator('#backpackButton').click({timeout:3000});await page.waitForTimeout(40);assert.equal(await page.locator('#backpackPanel').evaluate(el=>el.classList.contains('open')),false,'living-cargo backpack did not close');

assert.equal(await page.locator('#rail [data-organ="bag"]').count(),0,'standalone canonical backpack must not be duplicated in the organ rail');assert.equal(await page.locator('#rail [data-organ]').count(),13,'organ rail must retain every non-duplicate current organ');
await page.locator('#dockToggle').click({timeout:3000});await page.screenshot({path:`${ARTIFACT_DIR}/11520-mobile-dock-open.png`,fullPage:true});for(const id of ['trade','positions','orders','history','assets','records','market','character','world','worldmap','atm','settings','help']){const target=page.locator(`#rail [data-organ="${id}"]`);const hit=await target.evaluate(el=>{const r=el.getBoundingClientRect(),top=document.elementFromPoint(r.x+r.width/2,r.y+r.height/2);return top===el||el.contains(top)});assert.equal(hit,true,`organ ${id} is visible but pointer-blocked`);await target.click({timeout:3000});await page.waitForTimeout(20);assert.equal(await page.locator('#sheet').evaluate(el=>el.classList.contains('open')),true,`organ ${id} did not open`);await page.locator('#sheetClose').click({timeout:3000});await page.locator('#dockToggle').click({timeout:3000})}
assert.equal(await page.locator('#dock').evaluate(el=>el.classList.contains('open')),true,'dock should be open after organ loop');await page.locator('#dockToggle').click({timeout:3000});await page.waitForTimeout(80);assert.equal(await page.locator('#dock').evaluate(el=>el.classList.contains('open')),false,'dock must close before standalone shortcuts resume');

assert.deepEqual(errors,[],'page errors: '+errors.join('\n'));await browser.close();console.log('11520 V2.6.17 mobile functional + visual geometry + settings + living-cargo backpack smoke PASS');
