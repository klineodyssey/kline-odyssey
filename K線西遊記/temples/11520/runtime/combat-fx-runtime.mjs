/* KGEN_META
VERSION: 2.1.1
STATUS: ACTIVE
PURPOSE: Presentation-only three-style combat FX: slash, golden target/rain, phantom axe.
*/
export function create11520CombatFx(THREE,{scene,camera,avatar}={}){
 if(!THREE||!scene||!camera||!avatar)return{trigger(){},tick(){},applyCameraShake(){}};
 let audio=null,shakeUntil=0,shakePower=0,flash=null,uiShieldTimer=0;
 if(typeof document!=='undefined'&&!document.getElementById('k11520CombatFxHudShield')){const style=document.createElement('style');style.id='k11520CombatFxHudShield';style.textContent=`html.k11520CombatFxActive .top,html.k11520CombatFxActive .axis,html.k11520CombatFxActive .tele,html.k11520CombatFxActive .monsterHud,html.k11520CombatFxActive .minimapWrap{background:#091119!important;background-image:none!important;backdrop-filter:none!important}`;document.head.appendChild(style)}
 function shieldHud(duration){if(typeof document==='undefined')return;document.documentElement.classList.add('k11520CombatFxActive');clearTimeout(uiShieldTimer);uiShieldTimer=setTimeout(()=>document.documentElement.classList.remove('k11520CombatFxActive'),duration)}
 const dustGeo=new THREE.BufferGeometry(),dustPos=new Float32Array(192);for(let i=0;i<64;i++){dustPos[i*3]=(Math.random()-.5)*36;dustPos[i*3+1]=.4+Math.random()*5;dustPos[i*3+2]=(Math.random()-.5)*36}dustGeo.setAttribute('position',new THREE.BufferAttribute(dustPos,3));const dustMat=new THREE.PointsMaterial({size:.055,color:0x8defff,transparent:true,opacity:.28,depthWrite:false,blending:THREE.AdditiveBlending}),dust=new THREE.Points(dustGeo,dustMat);scene.add(dust);
 const mat=(color,opacity=1)=>new THREE.MeshBasicMaterial({color,transparent:true,opacity,side:THREE.DoubleSide,depthWrite:false,blending:THREE.AdditiveBlending});
 function flashNow(color){if(typeof document==='undefined')return;if(!flash){flash=document.createElement('div');flash.id='k11520CombatFlash';flash.style.cssText='position:fixed;z-index:6400;inset:0;pointer-events:none;opacity:0;mix-blend-mode:screen;transition:opacity .12s ease-out';document.body.appendChild(flash)}flash.style.background='radial-gradient(circle at 60% 55%,'+color+',transparent 60%)';flash.style.opacity='.72';setTimeout(()=>flash&&(flash.style.opacity='0'),55)}
 function sound(power=1,tone=220){try{const C=globalThis.AudioContext||globalThis.webkitAudioContext;if(!C)return;if(!audio)audio=new C();audio.resume?.();const now=audio.currentTime,g=audio.createGain(),o=audio.createOscillator(),o2=audio.createOscillator();o.type='sawtooth';o.frequency.setValueAtTime(tone,now);o.frequency.exponentialRampToValueAtTime(65,now+.24);o2.frequency.setValueAtTime(tone*3.2,now);o2.frequency.exponentialRampToValueAtTime(tone*.8,now+.18);g.gain.setValueAtTime(.0001,now);g.gain.exponentialRampToValueAtTime(.035*power,now+.012);g.gain.exponentialRampToValueAtTime(.0001,now+.28);o.connect(g);o2.connect(g);g.connect(audio.destination);o.start();o2.start();o.stop(now+.3);o2.stop(now+.3)}catch{}}
 function dispose(root){root.traverse(o=>{o.geometry?.dispose?.();o.material?.dispose?.()});scene.remove(root)}
 function slash(h){const root=new THREE.Group(),f=new THREE.Vector3(Math.sin(h),0,Math.cos(h));root.position.copy(avatar.position).addScaledVector(f,.8);root.position.y+=1.05;root.rotation.y=h;scene.add(root);const arc=new THREE.Mesh(new THREE.TorusGeometry(1.05,.045,6,42,Math.PI*1.35),mat(0x7deaff,.96));arc.rotation.set(Math.PI/2,.25,-.55);root.add(arc);const start=performance.now();shakeUntil=start+150;shakePower=.03;flashNow('rgba(104,228,255,.34)');sound(1,230);const go=now=>{const t=Math.min(1,(now-start)/330);arc.scale.setScalar(.7+t*.8);arc.material.opacity=1-t;arc.rotation.z=-.55+t*1.45;if(t<1)requestAnimationFrame(go);else dispose(root)};requestAnimationFrame(go)}
 function goldenRain(h,target){
  const root=new THREE.Group(),f=new THREE.Vector3(Math.sin(h),0,Math.cos(h));root.position.copy(avatar.position).addScaledVector(f,2.8);if(target)root.position.set(target.x,target.y,target.z);scene.add(root);
  const sigil=new THREE.Group();root.add(sigil);
  for(const radius of [1.05,1.4]){const ring=new THREE.Mesh(new THREE.RingGeometry(radius-.035,radius,64),mat(0xffd65c,.9));ring.rotation.x=-Math.PI/2;ring.position.y=.04;sigil.add(ring)}
  for(let i=0;i<8;i++){const rune=new THREE.Mesh(new THREE.BoxGeometry(.06,.02,.32),mat(0xffed9d,.9));const a=i*Math.PI/4;rune.position.set(Math.sin(a)*1.22,.055,Math.cos(a)*1.22);rune.rotation.y=a;sigil.add(rune)}
  const beams=[];for(let i=0;i<24;i++){const a=i*2.4,r=.25+(i%5)*.21,b=new THREE.Mesh(new THREE.CylinderGeometry(.022,.065,.65+(i%3)*.18,6),mat(i%4?0xffcb45:0xfff2bf,.85));b.position.set(Math.cos(a)*r,3,Math.sin(a)*r);root.add(b);beams.push(b)}
  const start=performance.now();shakeUntil=start+850;shakePower=.045;flashNow('rgba(255,214,92,.3)');sound(1.35,330);
  const go=now=>{const t=Math.min(1,(now-start)/1100);sigil.rotation.y=t*.35;for(const ring of sigil.children)ring.material.opacity=Math.min(1,t*10)*(1-t*.65);for(let i=0;i<beams.length;i++){const b=beams[i],fall=Math.max(0,(t-.12-(i%6)*.035)/.68);b.visible=t>=.12+(i%6)*.035&&fall<=1;b.position.y=1.8*(1-fall)+.22;b.material.opacity=.9*(1-Math.max(0,fall-.72)*3)}if(t<1)requestAnimationFrame(go);else dispose(root)};requestAnimationFrame(go)
 }
 function phantomAxe(h){const root=new THREE.Group(),f=new THREE.Vector3(Math.sin(h),0,Math.cos(h));root.position.copy(avatar.position).addScaledVector(f,1.4);root.position.y+=1.25;root.rotation.y=h;scene.add(root);const handle=new THREE.Mesh(new THREE.CylinderGeometry(.055,.075,3.6,8),mat(0xbbe9ff,.7));handle.rotation.z=Math.PI/2;root.add(handle);const sh=new THREE.Shape();sh.moveTo(.55,.2);sh.quadraticCurveTo(.9,.65,.65,1.05);sh.quadraticCurveTo(2.5,.95,2.45,0);sh.quadraticCurveTo(2.5,-.95,.65,-1.05);sh.quadraticCurveTo(.9,-.65,.55,-.2);sh.closePath();const blade=new THREE.Mesh(new THREE.ShapeGeometry(sh),mat(0x9ee7ff,.72));root.add(blade);const edge=new THREE.LineLoop(new THREE.BufferGeometry().setFromPoints(sh.getPoints(32)),new THREE.LineBasicMaterial({color:0xe9fbff,transparent:true,opacity:.95,depthWrite:false}));root.add(edge);const sweep=new THREE.Mesh(new THREE.TorusGeometry(2.2,.035,6,64,Math.PI*1.1),mat(0xd9f7ff,.35));sweep.rotation.x=Math.PI/2;sweep.rotation.z=-.8;root.add(sweep);const start=performance.now();shakeUntil=start+480;shakePower=.065;flashNow('rgba(184,235,255,.28)');sound(1.6,120);const go=now=>{const t=Math.min(1,(now-start)/780);root.rotation.z=-.9+t*2.1;root.scale.setScalar(.9+t*.25);blade.material.opacity=.72*(1-t*.82);edge.material.opacity=.95*(1-t*.82);sweep.material.opacity=.35*(1-t);if(t<1)requestAnimationFrame(go);else dispose(root)};requestAnimationFrame(go)}
 function trigger({variant='slash',heading=0,target=null}={}){shieldHud(variant==='goldenRain'?1150:variant==='phantomAxe'?830:380);if(variant==='goldenRain')goldenRain(heading,target);else if(variant==='phantomAxe')phantomAxe(heading);else slash(heading);globalThis.__K11520_COMBAT_FX__={variant,at:Date.now(),presentationOnly:true,hudShielded:true}}
 function tick(now,pos){dust.rotation.y=now*.000018;dust.position.x=Number(pos?.x)||0;dust.position.z=Number(pos?.z)||0;dustMat.opacity=.22+.08*Math.sin(now*.0012)}
 function applyCameraShake(now){if(now>=shakeUntil)return;const k=(shakeUntil-now)/650*shakePower;camera.position.x+=(Math.random()-.5)*k;camera.position.y+=(Math.random()-.5)*k*.7;camera.position.z+=(Math.random()-.5)*k}
 return{trigger,tick,applyCameraShake}
}
