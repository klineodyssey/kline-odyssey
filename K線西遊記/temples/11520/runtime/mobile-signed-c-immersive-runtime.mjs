/* KGEN_META
VERSION: 1.3.0
STATUS: ACTIVE / UI-ONLY
FORMAL_ORGAN_NAME: 11520 Signed C + Immersive Mobile Runtime
PURPOSE: Signed C velocity (+C=多, -C=空, 0C neutral), positive lots, configurable long/short colors, precise numeric entry, and bounded immersive mobile behavior. Direction/size editing never executes an order.
*/

const $=s=>document.querySelector(s);
const ROOT=document.documentElement;
const MOBILE_MAX=600;
const ABS_LEVELS=Object.freeze([0,0.000001,0.00001,0.0001,0.001,0.01,0.1,1,10,100,1000]);
const signedByAxis={KX:0.001,KY:0.001,KZ:0.001};
const sideSyncedByAxis={KX:'LONG',KY:'LONG',KZ:'LONG'};
const COLOR_STORE='k11520.trade.colorScheme';
const COLOR_SCHEMES=Object.freeze({
  TW_RED_LONG:Object.freeze({id:'TW_RED_LONG',long:'#ff4f5e',short:'#35d07f',neutral:'#d8e4eb'}),
  GLOBAL_GREEN_LONG:Object.freeze({id:'GLOBAL_GREEN_LONG',long:'#35d07f',short:'#ff4f5e',neutral:'#d8e4eb'})
});
let colorSchemeId='TW_RED_LONG';
let fallbackImmersive=false,lastError=null,cPointer=null,internalNative=false,busySide=false,timer=null;

const activeCard=()=>$('#axes [data-axis].active')||$('#axes [data-axis="KX"]');
const activeAxis=()=>activeCard()?.dataset.axis||'KX';
const fullEl=()=>document.fullscreenElement||document.webkitFullscreenElement||null;
const signSide=v=>v<0?'SHORT':v>0?'LONG':'NEUTRAL';
const parseSide=text=>/空|SHORT|SELL/i.test(String(text||''))?'SHORT':'LONG';
const formatC=v=>v===0?'0C':`${v>0?'+':''}${Number(v).toLocaleString(undefined,{maximumFractionDigits:6,useGrouping:false})}C`;
const formatInputC=v=>v===0?'0':`${v>0?'+':''}${Number(v).toLocaleString('en-US',{maximumFractionDigits:6,useGrouping:false})}`;
const viewportHeight=()=>Math.max(1,Math.round(globalThis.visualViewport?.height||innerHeight||document.documentElement.clientHeight||1));

function nearestAbsIndex(magnitude){let best=0;for(let i=1;i<ABS_LEVELS.length;i++)if(Math.abs(ABS_LEVELS[i]-magnitude)<Math.abs(ABS_LEVELS[best]-magnitude))best=i;return best}
function normalizeNumericC(raw){const n=Number(String(raw).replace(/C/ig,'').trim());if(!Number.isFinite(n))return null;const bounded=Math.max(-1000,Math.min(1000,n));if(Math.abs(bounded)<5e-7)return 0;return Math.sign(bounded)*ABS_LEVELS[nearestAbsIndex(Math.abs(bounded))]}
function signedFromPointer(clientY,rect){const centered=Math.max(-1,Math.min(1,1-2*((clientY-rect.top)/Math.max(1,rect.height))));if(Math.abs(centered)<.055)return 0;const i=Math.max(1,Math.round(Math.abs(centered)*(ABS_LEVELS.length-1)));return Math.sign(centered)*ABS_LEVELS[i]}
function signedThumbTop(value){if(value===0)return 50;const idx=nearestAbsIndex(Math.abs(value));return 50-Math.sign(value)*(idx/(ABS_LEVELS.length-1)*50)}
function readLots(){const n=parseInt($('#lotsRead')?.textContent||'1',10);return Number.isFinite(n)?Math.max(1,Math.min(100,n)):1}

function ensureStyle(){if($('#k11520SignedCImmersiveStyle'))return;const s=document.createElement('style');s.id='k11520SignedCImmersiveStyle';s.textContent=`
:root{--k11520-long-color:#ff4f5e;--k11520-short-color:#35d07f;--k11520-neutral-color:#d8e4eb}
html.k11520ImmersiveViewport,html.k11520ImmersiveViewport body{width:100vw!important;height:var(--k11520-visible-vh,100dvh)!important;min-height:var(--k11520-visible-vh,100dvh)!important;max-height:var(--k11520-visible-vh,100dvh)!important;overflow:hidden!important}
html.k11520ImmersiveViewport #three{width:100vw!important;height:var(--k11520-visible-vh,100dvh)!important;min-height:var(--k11520-visible-vh,100dvh)!important;max-height:var(--k11520-visible-vh,100dvh)!important}
#k11520ImmersiveExit{position:fixed;z-index:13050;right:4px;top:max(4px,env(safe-area-inset-top));width:30px;height:30px;border:1px solid #68e4ff66;border-radius:9px;background:#071018dd;color:#dffaff;font:900 15px system-ui;display:none;place-items:center;touch-action:manipulation;box-shadow:0 4px 18px #000b}
html.k11520ImmersiveViewport #k11520ImmersiveExit{display:grid}
#cControl .track{background:linear-gradient(to bottom,color-mix(in srgb,var(--k11520-long-color) 42%,#071018) 0%,color-mix(in srgb,var(--k11520-long-color) 42%,#071018) 49.2%,var(--k11520-neutral-color) 49.2%,var(--k11520-neutral-color) 50.8%,color-mix(in srgb,var(--k11520-short-color) 42%,#071018) 50.8%,color-mix(in srgb,var(--k11520-short-color) 42%,#071018) 100%)!important}
#cControl[data-c-sign="positive"]{border-color:var(--k11520-long-color)!important;box-shadow:0 0 16px color-mix(in srgb,var(--k11520-long-color) 30%,transparent)!important}
#cControl[data-c-sign="negative"]{border-color:var(--k11520-short-color)!important;box-shadow:0 0 16px color-mix(in srgb,var(--k11520-short-color) 30%,transparent)!important}
#cControl[data-c-sign="zero"]{border-color:color-mix(in srgb,var(--k11520-neutral-color) 55%,transparent)!important}
#cControl[data-c-sign="positive"] label,#cControl[data-c-sign="positive"] .read,#cControl[data-c-sign="positive"] #cNumericInput{color:var(--k11520-long-color)!important}
#cControl[data-c-sign="negative"] label,#cControl[data-c-sign="negative"] .read,#cControl[data-c-sign="negative"] #cNumericInput{color:var(--k11520-short-color)!important}
#cControl[data-c-sign="zero"] label,#cControl[data-c-sign="zero"] .read,#cControl[data-c-sign="zero"] #cNumericInput{color:var(--k11520-neutral-color)!important}
#orderFire[data-k11520-side="LONG"]{border-color:var(--k11520-long-color)!important}#orderFire[data-k11520-side="SHORT"]{border-color:var(--k11520-short-color)!important}
#k11520TradeColorRow{grid-template-columns:1fr 116px!important}#k11520TradeColorScheme{width:116px;height:25px;border:1px solid #68e4ff55;border-radius:7px;background:#10202d;color:#e9fbff;font:800 8px system-ui,"Noto Sans TC",sans-serif;padding:0 4px}
#cControl,#lotsControl{overflow:visible!important}.k11520NumericEditor{position:absolute;left:50%;bottom:-24px;transform:translateX(-50%);width:48px;height:20px;z-index:4;border:1px solid #68e4ff55;border-radius:6px;background:#071018f2;color:#e9fbff;text-align:center;padding:0 2px;font:800 8px ui-monospace,SFMono-Regular,Consolas,monospace;outline:none;touch-action:manipulation}.k11520NumericEditor:focus{border-color:#dffaff;box-shadow:0 0 0 1px #dffaff55}
@media(max-width:${MOBILE_MAX}px){html.k11520ImmersiveViewport body{overscroll-behavior:none!important}}
`;document.head.appendChild(s)}

function loadColorScheme(){try{const saved=localStorage.getItem(COLOR_STORE);if(saved&&COLOR_SCHEMES[saved])colorSchemeId=saved}catch{}return colorSchemeId}
function setColorScheme(id,{persist=true}={}){colorSchemeId=COLOR_SCHEMES[id]?id:'TW_RED_LONG';const s=COLOR_SCHEMES[colorSchemeId];ROOT.style.setProperty('--k11520-long-color',s.long);ROOT.style.setProperty('--k11520-short-color',s.short);ROOT.style.setProperty('--k11520-neutral-color',s.neutral);ROOT.dataset.k11520TradeColorScheme=colorSchemeId;const select=$('#k11520TradeColorScheme');if(select&&select.value!==colorSchemeId)select.value=colorSchemeId;if(persist)try{localStorage.setItem(COLOR_STORE,colorSchemeId)}catch{}paintSignedC();publish();return colorSchemeId}
function ensureColorSetting(){const panel=$('#k11520UiSettings');if(!panel)return false;let row=$('#k11520TradeColorRow');if(!row){row=document.createElement('label');row.id='k11520TradeColorRow';row.className='row full k11520TradeColorRow';row.innerHTML='<span>多空配色</span><select id="k11520TradeColorScheme" aria-label="多空配色"><option value="TW_RED_LONG">台股｜多紅 空綠</option><option value="GLOBAL_GREEN_LONG">國際｜多綠 空紅</option></select>';panel.appendChild(row);row.querySelector('select').addEventListener('change',e=>setColorScheme(e.target.value))}const select=$('#k11520TradeColorScheme');if(select&&select.value!==colorSchemeId)select.value=colorSchemeId;return true}

function syncViewport(){const next=`${viewportHeight()}px`;if(ROOT.style.getPropertyValue('--k11520-visible-vh')!==next)ROOT.style.setProperty('--k11520-visible-vh',next)}
function immersiveOn(){return !!fullEl()||fallbackImmersive}
function syncImmersive(){syncViewport();ROOT.classList.toggle('k11520ImmersiveViewport',immersiveOn());ROOT.dataset.k11520ImmersiveMode=fullEl()?'fullscreen':fallbackImmersive?'viewport-fallback':'off';const sw=$('#k11520FullscreenSwitch');if(sw){sw.dataset.k11520SignedCBound='1';sw.setAttribute('aria-checked',String(immersiveOn()));const label=sw.closest('.row')?.querySelector('span');if(label&&label.textContent!=='沉浸全螢幕')label.textContent='沉浸全螢幕'}const exit=$('#k11520ImmersiveExit');if(exit){const t=fullEl()?'退出全螢幕':'退出沉浸模式';exit.title=t;exit.setAttribute('aria-label',t)}publish()}
async function requestImmersiveFullscreen(){lastError=null;try{if(fullEl())return true;const el=document.documentElement;if(typeof el.requestFullscreen==='function')await el.requestFullscreen({navigationUI:'hide'});else if(typeof el.webkitRequestFullscreen==='function')await el.webkitRequestFullscreen();else throw new Error('FULLSCREEN_API_UNAVAILABLE');fallbackImmersive=false;syncImmersive();return true}catch(err){lastError=String(err?.message||err||'FULLSCREEN_REJECTED');fallbackImmersive=true;syncImmersive();return false}}
async function exitImmersive(){try{if(document.fullscreenElement&&document.exitFullscreen)await document.exitFullscreen();else if(document.webkitFullscreenElement&&document.webkitExitFullscreen)await document.webkitExitFullscreen()}catch(err){lastError=String(err?.message||err)}fallbackImmersive=false;syncImmersive();return true}
async function toggleImmersive(){return immersiveOn()?exitImmersive():requestImmersiveFullscreen()}
function setFallbackImmersive(on=true){fallbackImmersive=!!on;syncImmersive();return immersiveOn()}
function ensureExit(){let b=$('#k11520ImmersiveExit');if(!b){b=document.createElement('button');b.id='k11520ImmersiveExit';b.type='button';b.textContent='↙';document.body.appendChild(b)}if(!b.dataset.bound){b.dataset.bound='1';b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();void exitImmersive()},{capture:true})}return b}
function bindFullscreenSwitch(){const b=$('#k11520FullscreenSwitch');if(!b||b.dataset.k11520SignedCBound)return !!b;b.dataset.k11520SignedCBound='1';b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();void toggleImmersive()},{capture:true});syncImmersive();return true}

function dispatchNativeMagnitude(magnitude){const el=$('#cControl');if(!el)return;const r=el.getBoundingClientRect(),idx=nearestAbsIndex(Math.abs(magnitude)),t=idx/(ABS_LEVELS.length-1),y=r.top+(1-t)*r.height,x=r.left+r.width/2;internalNative=true;try{for(const [type,buttons] of [['pointerdown',1],['pointerup',0]])el.dispatchEvent(new PointerEvent(type,{bubbles:true,cancelable:true,pointerId:911,pointerType:'touch',clientX:x,clientY:y,buttons}))}finally{internalNative=false}}
function dispatchNativeLots(lots){const el=$('#lotsControl');if(!el)return;const r=el.getBoundingClientRect(),t=Math.max(.01,Math.min(1,Number(lots)/100)),y=r.top+(1-t)*r.height,x=r.left+r.width/2;for(const [type,buttons] of [['pointerdown',1],['pointermove',1],['pointerup',0]])el.dispatchEvent(new PointerEvent(type,{bubbles:true,cancelable:true,pointerId:912,pointerType:'touch',clientX:x,clientY:y,buttons}))}
function syncNumericEditors(){const c=$('#cNumericInput'),lots=$('#lotsNumericInput'),v=Number(signedByAxis[activeAxis()]||0);if(c&&document.activeElement!==c)c.value=formatInputC(v);if(lots&&document.activeElement!==lots)lots.value=String(readLots())}
function setCFromNumeric(raw){const v=normalizeNumericC(raw);if(v==null){syncNumericEditors();return null}applySignedValue(v);const input=$('#cNumericInput');if(input)input.value=formatInputC(v);return v}
function setLotsFromNumeric(raw){const n=Number(String(raw).replace(/口/g,'').trim());if(!Number.isFinite(n)){syncNumericEditors();return null}const lots=Math.max(1,Math.min(100,Math.round(n))),input=$('#lotsNumericInput');if(input)input.value=String(lots);dispatchNativeLots(lots);queueMicrotask(syncNumericEditors);setTimeout(syncNumericEditors,50);publish();return lots}
function bindNumericEditor(input,commit){if(input.dataset.k11520NumericBound)return;input.dataset.k11520NumericBound='1';for(const type of ['pointerdown','pointermove','pointerup','pointercancel','touchstart','touchmove','touchend'])input.addEventListener(type,e=>e.stopPropagation(),{passive:true});input.addEventListener('keydown',e=>{e.stopPropagation();if(e.key==='Enter'){e.preventDefault();commit(input.value);input.blur()}else if(e.key==='Escape'){e.preventDefault();syncNumericEditors();input.blur()}});input.addEventListener('change',()=>commit(input.value));input.addEventListener('blur',()=>{commit(input.value);syncNumericEditors()})}
function ensureNumericEditors(){const cc=$('#cControl'),lc=$('#lotsControl');if(!cc||!lc)return false;let c=$('#cNumericInput');if(!c){c=document.createElement('input');c.id='cNumericInput';c.className='k11520NumericEditor';c.type='text';c.inputMode='decimal';c.autocomplete='off';c.spellcheck=false;c.setAttribute('aria-label','C 速度精準輸入；可輸入正負值');c.title='輸入 -1000 ～ +1000；依目前 C 能階取最近可用值';cc.appendChild(c)}let l=$('#lotsNumericInput');if(!l){l=document.createElement('input');l.id='lotsNumericInput';l.className='k11520NumericEditor';l.type='number';l.inputMode='numeric';l.min='1';l.max='100';l.step='1';l.autocomplete='off';l.setAttribute('aria-label','口數精準輸入；1 到 100 正整數');l.title='輸入 1 ～ 100 口；口數永遠為正';lc.appendChild(l)}bindNumericEditor(c,setCFromNumeric);bindNumericEditor(l,setLotsFromNumeric);syncNumericEditors();return true}

function renderTradeSideLock(){if(busySide)return;const side=$('#sideBtn');if(!side)return;const v=Number(signedByAxis[activeAxis()]||0),wanted=signSide(v),text=`方向：${wanted==='LONG'?'多':wanted==='SHORT'?'空':'中性'}（由 ${formatC(v)}）`;if(side.textContent!==text)side.textContent=text;if(!side.disabled)side.disabled=true;if(side.dataset.k11520Side!==wanted)side.dataset.k11520Side=wanted;if(side.getAttribute('aria-disabled')!=='true')side.setAttribute('aria-disabled','true');side.title='多空由 C 正負唯一決定；不可另外切換'}
function paintSignedC(axis=activeAxis()){const v=Number(signedByAxis[axis]||0),control=$('#cControl'),read=$('#cRead'),thumb=$('#cThumb');if(!control||!read||!thumb)return false;const sign=v>0?'positive':v<0?'negative':'zero';control.dataset.cSign=sign;control.dataset.cSide=signSide(v);control.setAttribute('aria-label',`C 速度 ${formatC(v)}；上正、中央 0、下負；${v>0?'多':v<0?'空':'中性'}`);control.title=control.getAttribute('aria-label');const text=formatC(v);if(read.textContent!==text)read.textContent=text;const top=`${signedThumbTop(v)}%`;if(thumb.style.top!==top)thumb.style.top=top;const label=control.querySelector('label');if(label&&label.textContent!=='C 速度')label.textContent='C 速度';const order=$('#orderFire');if(order){order.dataset.k11520SignedC=text;order.dataset.k11520Side=signSide(v);order.setAttribute('aria-label',`下單確認：${axis} ${text} ${v>0?'多':v<0?'空':'中性'}；方向由 C 正負決定`)}renderTradeSideLock();syncNumericEditors();publish();return true}
async function syncCanonicalSide(value){const wanted=signSide(value),axis=activeAxis();if(wanted==='NEUTRAL'||busySide||sideSyncedByAxis[axis]===wanted)return true;busySide=true;ROOT.classList.add('k11520SignedCSideSync');try{const trade=$('[data-organ="trade"]');if(!trade)throw new Error('TRADE_ORGAN_NOT_FOUND');trade.click();await new Promise(r=>requestAnimationFrame(r));let side=$('#sideBtn');if(!side)throw new Error('SIDE_BUTTON_NOT_FOUND');side.disabled=false;side.removeAttribute('aria-disabled');if(parseSide(side.textContent)!==wanted){side.click();await new Promise(r=>requestAnimationFrame(r));side=$('#sideBtn')||side}if(parseSide(side.textContent)!==wanted)throw new Error('SIDE_SYNC_FAILED');sideSyncedByAxis[axis]=wanted;$('#sheetClose')?.click();await new Promise(r=>requestAnimationFrame(r));lastError=null;return true}catch(err){lastError=String(err?.message||err);return false}finally{ROOT.classList.remove('k11520SignedCSideSync');busySide=false;renderTradeSideLock();publish()}}
function applySignedValue(value,{syncSide=true}={}){const axis=activeAxis(),v=Object.is(value,-0)?0:value;signedByAxis[axis]=v;dispatchNativeMagnitude(Math.abs(v));if(syncSide&&v!==0)void syncCanonicalSide(v);paintSignedC(axis);return v}
function onCEvent(e){if(internalNative||e.target?.classList?.contains('k11520NumericEditor'))return;const el=e.target?.closest?.('#cControl');if(!el)return;if(e.type==='pointerdown'){cPointer=e.pointerId;try{el.setPointerCapture?.(cPointer)}catch{}}if(e.type==='pointermove'&&e.pointerId!==cPointer)return;if((e.type==='pointerup'||e.type==='pointercancel')&&e.pointerId!==cPointer)return;e.preventDefault();e.stopImmediatePropagation();if(e.type!=='pointerup'&&e.type!=='pointercancel')applySignedValue(signedFromPointer(e.clientY,el.getBoundingClientRect()));else cPointer=null}
function onDocumentClick(e){if(e.target?.id==='sideBtn'&&!busySide){e.preventDefault();e.stopImmediatePropagation();renderTradeSideLock();return}const card=e.target?.closest?.('#axes [data-axis]');if(card)setTimeout(()=>{paintSignedC(card.dataset.axis);const v=Number(signedByAxis[card.dataset.axis]||0);if(v!==0)void syncCanonicalSide(v);syncNumericEditors()},0)}
function bindC(){const el=$('#cControl');if(!el||el.dataset.k11520SignedCBound)return !!el;el.dataset.k11520SignedCBound='1';for(const type of ['pointerdown','pointermove','pointerup','pointercancel'])el.addEventListener(type,onCEvent,{capture:true,passive:false});paintSignedC();return true}

function publish(){const scheme=COLOR_SCHEMES[colorSchemeId],v=Number(signedByAxis[activeAxis()]||0),wanted=signSide(v);globalThis.__K11520_SIGNED_C_IMMERSIVE__={version:'1.3.0',ready:true,activeAxis:activeAxis(),signedC:v,signedByAxis:{...signedByAxis},lots:readLots(),lotsRemainPositive:true,cSemantics:'SIGNED_VELOCITY_PLUS_LONG_MINUS_SHORT',canonicalSideSynced:wanted==='NEUTRAL'||sideSyncedByAxis[activeAxis()]===wanted,numericEntry:{c:true,lots:true,cRange:[-1000,1000],lotsRange:[1,100],cCanonicalLevels:[...ABS_LEVELS]},colorScheme:colorSchemeId,colorSemantics:{long:scheme.long,short:scheme.short,neutral:scheme.neutral},fullscreen:!!fullEl(),fallbackImmersive,immersive:immersiveOn(),navigationUiRequested:'hide',lastError,api:{applySignedValue,setCFromNumeric,setLotsFromNumeric,setColorScheme,setFallbackImmersive,requestImmersiveFullscreen,exitImmersive,toggleImmersive,paintSignedC}}}
function install(){if(typeof document==='undefined')return null;ensureStyle();loadColorScheme();setColorScheme(colorSchemeId,{persist:false});ensureExit();bindFullscreenSwitch();bindC();ensureColorSetting();ensureNumericEditors();syncImmersive();paintSignedC();if(!ROOT.dataset.k11520SignedCGlobalBound){ROOT.dataset.k11520SignedCGlobalBound='1';document.addEventListener('click',onDocumentClick,true);document.addEventListener('fullscreenchange',syncImmersive);document.addEventListener('webkitfullscreenchange',syncImmersive);addEventListener('resize',()=>{syncViewport();paintSignedC()},{passive:true});globalThis.visualViewport?.addEventListener?.('resize',syncViewport,{passive:true})}clearInterval(timer);timer=setInterval(()=>{bindFullscreenSwitch();bindC();ensureColorSetting();ensureNumericEditors();renderTradeSideLock();syncNumericEditors();publish()},250);publish();return globalThis.__K11520_SIGNED_C_IMMERSIVE__}

export function install11520SignedCImmersive(){return install()}
if(typeof document!=='undefined')install();
