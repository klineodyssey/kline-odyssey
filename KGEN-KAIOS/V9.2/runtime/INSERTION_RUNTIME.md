# Insertion Runtime

**Runtime ID:** KAIOS-V9.2-RUNTIME-INSERTION  
**Status:** Prototype  
**Mode:** Codex-only file update.

The Insertion Runtime appends a formal `OPEN` WorkOrder into `KGEN-Organization/WorkOrders/WORK_QUEUE.md` without changing existing task order or status.

V9.2 insertion may include `Dispatch Hold: true` to prevent auto-claim.
